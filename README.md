# Interactive Greeting Program in C

A clean, safe, and interactive C program that requests a user's name.

## Features
- Uses fgets() instead of scanf() to read full names with spaces safely.
- Cleans up trailing newline characters using strcspn().

## Compilation
```bash
gcc main.c -o greeting
```

## Running
```bash
./greeting
```
